#include<stdio.h>

void merge(a,p,q,r)
{
    int n1,n2;
    int i,l,r,j;
    n1 = q-p+1;
    n2 = r-p;


    for(i=1;i<=n1;i++)
    {
        l[i]=a[p+i-1];
    }
    for(i=1;i<=n2;i++)
    {
        r[j]=a[q+j];
    }


}
int main()
{
   void merge-sort(a,p,r)
{
    if (p<r)
        q = [(p+r)/2];
        merge-sort(a,p,q);
        merge-sort(a,q+1,r);
        merge(a,p,q,r);
}
return 0;
}
